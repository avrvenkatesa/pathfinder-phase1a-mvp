#### 5. US-W001: Create Basic Sequential Workflow
**Priority**: P0 - Core Workflow Feature
**Status**: ❌ Not Started
**Dependencies**: US-DV001

**Replit Prompt**:
```
Build a workflow designer and execution engine:

1. Create workflow data model:
   - Workflow definition table (name, description, version, status)
   - Workflow steps table (sequence, name, type, assignee, duration)
   - Step dependencies table (predecessor, successor, dependency type)
   - Workflow instances table (workflow_id, status, started_at, completed_at)
   - Step instances table (step_id, instance_id, status, assigned_to)

2. Build drag-and-drop workflow designer:
   - Use @dnd-kit for drag-and-drop functionality
   - Step library panel with draggable components
   - Canvas area for workflow design
   - Properties panel for step configuration
   - Connection lines between steps
   - Auto-layout for sequential workflows

3. Implement step types:
   - Task (assignable to contact)
   - Approval (requires designated approver)
   - Notification (sends email/alert)
   - Conditional (if-then branching)
   - Timer (wait for duration)

4. Create workflow execution engine:
   - Start workflow instance from template
   - Assign tasks to contacts
   - Track step completion
   - Handle step transitions
   - Update workflow status
   - Calculate progress percentage

5. Add BPMN 2.0 export/import:
   - Convert workflow to BPMN XML
   - Import BPMN files
   - Validate BPMN compliance
   - Display BPMN notation icons

6. Build workflow runtime UI:
   - Active workflows dashboard
   - Step status tracker
   - Assignee task list
   - Workflow progress visualization

Include REST APIs for all workflow operations.